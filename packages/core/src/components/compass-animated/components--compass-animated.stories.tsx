import { CompassAnimated } from "./compass-animated";

export const XSmall = () => {
  return <CompassAnimated variants={{ size: "x-small" }} />;
};

export const Small = () => {
  return <CompassAnimated variants={{ size: "small" }} />;
};

export const Medium = () => {
  return <CompassAnimated variants={{ size: "medium" }} />;
};

export const Large = () => {
  return <CompassAnimated variants={{ size: "large" }} />;
};
