import { style } from '@pathfinder-ide/style';

export const panesStyles = {
  container: style({
    height: '100%',
    display: 'flex',
    justifyContent: 'flex-end',
    overflowY: 'auto',
  }),
};
