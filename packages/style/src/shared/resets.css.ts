import { style } from '@vanilla-extract/css';

export const buttonReset = style({
  all: 'unset',
  cursor: 'pointer',
  border: 'none',
  backgroundColor: 'transparent',
  margin: 0,
  padding: 0,
});
