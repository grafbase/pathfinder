import { hairlineBorder } from "./hairline-border.css";
import { buttonReset } from "./resets.css";
import { scrollbars } from "./scrollbars.css";
import { authenticMotion } from "./transitions";

export const shared = {
  hairlineBorder,
  resets: {
    buttonReset,
  },
  scrollbars,
  transitions: {
    authenticMotion,
  },
};
