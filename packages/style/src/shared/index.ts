import { buttonReset } from "./resets.css";
import { scrollbars } from "./scrollbars.css";
import { authenticMotion } from "./transitions";

export const shared = {
  resets: {
    buttonReset,
  },
  scrollbars,
  transitions: {
    authenticMotion,
  },
};
